/// Set panic hook for better error messages in WASM
pub fn set_panic_hook() {
    // In production, you could add console_error_panic_hook here
    // For now, we keep it minimal to reduce WASM size
}
